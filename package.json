{
  "name": "@overmindbots/core",
  "private": true,
  "engines": {
    "node": ">=9.0.0"
  },
  "scripts": {
    "commit": "git-cz",
    "commitmsg": "commitlint -E GIT_PARAMS",
    "release-production": "lerna publish",
    "release-staging": "lerna publish --canary",
    "bootstrap": "lerna bootstrap",
    "start-bot-referral-ranks": "lerna run start --scope={@overmindbots/bot-referral-ranks,@overmindbots/service-bot-manager} --parallel",
    "semantic-release": "semantic-release"
  },
  "devDependencies": {
    "@commitlint/cli": "^7.0.0",
    "@commitlint/config-conventional": "^7.0.1",
    "@commitlint/prompt": "^7.0.0",
    "commitizen": "^2.10.1",
    "cz-lerna-changelog": "^1.2.1",
    "git-branch-is": "^2.0.0",
    "husky": "^0.14.3",
    "lerna": "^2.11.0",
    "lerna-changelog": "^0.8.0",
    "semantic-release": "git+ssh://git@github.com:semantic-release/semantic-release.git#multi-branches",
    "semantic-release-cli": "^4.0.6"
  },
  "config": {
    "commitizen": {
      "path": "cz-lerna-changelog"
    }
  },
  "workspaces": [
    "packages/core/*",
    "packages/shared/*",
    "packages/public/*"
  ],
  "version": "0.0.0-development",
  "repository": {
    "type": "git",
    "url": "https://github.com/overmindbots/core.git"
  },
  "publishConfig": {
    "access": "restricted"
  }
}
